<h1>Listado de Facturas</h1>

<table class="table table-striped">
  <tr>
    <th>Folio</th>
    <th>Socio Comercial</th>
    <th>Cliente</th>
    <th>Total</th>
    <th>Estado</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @invoices.each do |invoice| %>
  <tr>
    <td><%= invoice.folio %></td>
    <td><%= invoice.member.name_member %></td>
    <td><%= invoice.client.name_client %></td>
    <td><%= invoice.total %></td>
    <td><%= invoice.status %></td>
    <td><%= link_to 'Show', invoice_path(invoice), class: "btn btn-mini"  %></td>
    <td><%= link_to 'Edit', edit_invoice_path(invoice) , class: "btn btn-primary btn-mini" %></td>
    <td><%= link_to 'Destroy', invoice , method: :delete, data: { confirm: '¿Estas seguro?' } , class: "btn btn-danger btn-mini" %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Factura', new_invoice_path %>
