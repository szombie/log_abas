	<fieldset>
	<%= f.input :product_id, :collection => Product.all(:order => 'key'),:label_method =>  lambda { |product| "#{product.key} | #{product.product_name}" } ,:wrapper_html => { :class => 'producto' }   %>
      <%= f.input :count , :wrapper_html => { :class => 'cantidad' } ,  :input_html => { :class => 'count' } %>
      <%= f.hidden_field :_destroy %>
     <div class="remove_producto" ><%= link_to "Quitar Producto" , "#", class: "remove_fields btn " %></div>
     </fieldset>